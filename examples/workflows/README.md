# Workflow Examples

This directory contains end-to-end examples for NexAU Workflow orchestration.

- `qa_release_check/`: a real LLM-backed QA release gate workflow with agent
  nodes, an external human-review subgraph, conditional routing, a loop, state
  updates, scoped events, and durable resume.
